{
    "title": "user_roles",
    "type": "object",
    "properties": {
        "id": {
            "type": "integer",
            "description": "Primary key, auto-increment"
        },
        "user_id": {
            "type": "string",
            "description": "User ID from Atoms auth"
        },
        "role": {
            "type": "string",
            "description": "Role: admin or viewer"
        },
        "email": {
            "type": "string",
            "description": "User email for display"
        }
    },
    "required": [
        "id",
        "user_id",
        "role"
    ]
}